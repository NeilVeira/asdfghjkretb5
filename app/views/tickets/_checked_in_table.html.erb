<b>Checked-in</b>
<br>
<%
  if list1.any?
%>
  <table class="table">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Ticket Type</th>
    </tr>

    <% list1.each do |t| %>
        <tr>
          <td><%=  t.firstname %></td>
          <td><%=  t.lastname %></td>
          <td><%=  get_tickettype_name (Ticket.where(tournament_id: id, person_id: t.id).first.tickettype)  %></td>
        </tr>
    <% end %>
  </table>
<% else %>
    No checked-in attendees yet.
<% end %>

<br>
<br>

<b>To Be Checked-in</b> <br>
<%
    if list2.any?
%>
  <table class="table">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Ticket Type</th>
    </tr>

    <% list2.each do |t| %>
        <tr>
          <td><%=  t.firstname %></td>
          <td><%=  t.lastname %></td>
          <td><%=  get_tickettype_name (Ticket.where(tournament_id: id, person_id: t.id).first.tickettype)  %></td>
        </tr>
    <% end %>
  </table>
<% else %>
    All attendees are checked-in.
<% end %>